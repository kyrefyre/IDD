import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { ICamera, IRenderTarget } from 'webgi/interfaces';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export interface VirtualCamera {
    camera: ICamera;
    target: IRenderTarget;
    enabled: boolean;
}
export declare class VirtualCamerasPlugin extends AViewerPlugin<'preRenderCamera' | 'preBlitCamera' | 'postRenderCamera'> {
    static readonly PluginType = "VirtualCamerasPlugin";
    enabled: boolean;
    toJSON: any;
    constructor(enabled?: boolean);
    cameras: VirtualCamera[];
    onAdded(viewer: ViewerApp): Promise<void>;
    protected _preRender: () => void;
    addCamera(camera: ICamera): VirtualCamera;
}
//# sourceMappingURL=VirtualCamerasPlugin.d.ts.map