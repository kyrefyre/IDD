import { Camera } from 'three';
import { IUiConfigContainer } from 'webgi/interfaces';
import { AHelperWidget } from 'webgi/extras/interaction/AHelperWidget';
export declare abstract class ACameraHelperWidget extends AHelperWidget {
    camera: (Camera & IUiConfigContainer) | undefined;
    protected constructor(object: Camera & IUiConfigContainer);
}
//# sourceMappingURL=ACameraHelperWidget.d.ts.map