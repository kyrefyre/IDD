export declare class LicenseVerification {
    private readonly PRODUCT_ID;
    private readonly KEY_TYPE_ID;
    private readonly TOKEN_FILE;
    private readonly PUBLIC_KEY_FILE;
    private readonly KEY_SERVER;
    private readonly KEY_SERVER_VERIFY;
    private readonly KEY_SERVER_CERTS;
    private id;
    private idHash;
    private appVersion;
    private appName;
    constructor(appName: string, appVersion: string);
    private _getOriginId;
    private _storage;
    private _getStorage;
    init(): Promise<{
        token: string;
        publicKey: JsonWebKey | null;
    }>;
    verify(key: string, data?: any): Promise<boolean>;
    writeToken(token: string): Promise<void>;
    verifyData(data: {
        payload: any;
    }): Promise<boolean>;
    protected _getPublicKeyFromServer(): Promise<{
        jwk?: JsonWebKey;
        error?: string;
    }>;
    protected _validateKeyFromServer(key: string): Promise<any>;
    silent: boolean;
    private _throw;
    setLicenseKey(key: string): Promise<any>;
    getPublicKey(): Promise<JsonWebKey | null>;
}
//# sourceMappingURL=index.d.ts.map